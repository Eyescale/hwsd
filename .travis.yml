# generated by Buildyard, do not edit.
notifications:
  email:
    on_success: never
language: cpp
script:
 - git clone --depth 10 https://github.com/Eyescale/Buildyard.git
 - cd Buildyard
 - git clone --depth 10 https://github.com/Eyescale/config.git config.eyescale
 - env TRAVIS=1 make -j8 hwsd-test ARGS=-V
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake git git-svn libavahi-compat-libdnssd-dev libboost-program-options-dev libboost-regex-dev libboost-serialization-dev libgl1-mesa-dev libhwloc-dev libjpeg-turbo8-dev libqt4-dev libturbojpeg libx11-dev pkg-config subversion 
