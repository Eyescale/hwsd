# generated by Buildyard, do not edit.
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq libavahi-compat-libdnssd-dev libboost-all-dev libgl1-mesa-dev libhwloc-dev libqt4-dev libx11-dev 
language: cpp
script:
 - git clone --depth 10 https://github.com/Eyescale/Buildyard.git
 - cd Buildyard
 - git clone --depth 10 https://github.com/Eyescale/config.git config.eyescale
 - make hwsd-test
